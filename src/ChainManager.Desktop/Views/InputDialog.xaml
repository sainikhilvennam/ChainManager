<Window x:Class="ChainManager.Desktop.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="{Binding Message}" TextWrapping="Wrap" Margin="0,0,0,15"/>
        
        <TextBox Grid.Row="1" x:Name="InputTextBox" Text="{Binding InputValue, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Click="OK_Click" IsDefault="True" Padding="15,5" Margin="0,0,10,0"/>
            <Button Content="Cancel" Click="Cancel_Click" IsCancel="True" Padding="15,5"/>
        </StackPanel>
    </Grid>
</Window>